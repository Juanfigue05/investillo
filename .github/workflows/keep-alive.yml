name: Keep Alive

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render (mantiene despierto el servidor Y la base de datos)
        run: curl -sf https://investillo.onrender.com/api/healthz-db